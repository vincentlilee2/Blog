# 依赖
node_modules/

# 构建缓存
dist/
.astro/
admin/admin-dist/

# 本地站点配置（含个人信息，不进仓库）
site-config.json
# 多用户子站 base 前缀（个人部署路径，不进仓库）
admin/server-config.json
# 部署配置（含服务器 IP / 域名，私密，不进仓库）
deploy-config.json

# 环境变量 / 密钥
.env
.env.*

# 系统文件
.DS_Store
Thumbs.db

# 编辑器
.idea/
.vscode/
*.swp
